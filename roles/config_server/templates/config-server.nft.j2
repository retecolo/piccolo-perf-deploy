table inet piccolo_fleet_config {
  set allowed_ifaces {
    type ifname
    elements = { lo, {{ allowed_mesh_ifaces | join(', ') }} }
  }

  chain input {
    type filter hook input priority filter; policy accept;
    iifname @allowed_ifaces accept
    tcp dport {{ config_server_port }} drop
  }
}
